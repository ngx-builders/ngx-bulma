import { BoxDirective } from './box.directive';

describe('BoxDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxDirective();
    expect(directive).toBeTruthy();
  });
});
