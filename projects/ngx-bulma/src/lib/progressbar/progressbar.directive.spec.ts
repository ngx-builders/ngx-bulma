import { ProgressbarDirective } from './progressbar.directive';

describe('ProgressbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressbarDirective();
    expect(directive).toBeTruthy();
  });
});
