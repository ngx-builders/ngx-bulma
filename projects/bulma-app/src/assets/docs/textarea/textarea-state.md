#### It comes with different states of the `Textarea`.We just need to use flags to enable them and ready to use.

###### `:hover` use `hovered=true`

###### `:focus` use `focus=true`

###### `:Fixed Size` use `fixed=true`
